// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyC3njcpG6Frk7UDd9QDoCKcuVgMmD1KfzA",
  authDomain: "friendlyeats-codelab-5661b.firebaseapp.com",
  projectId: "friendlyeats-codelab-5661b",
  storageBucket: "friendlyeats-codelab-5661b.firebasestorage.app",
  messagingSenderId: "422968366405",
  appId: "1:422968366405:web:8bd5656726fbe4718c9a2c"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

Test mode:

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if
          request.time < timestamp.date(2026, 6, 29);
    }
  }
}

Production mode: 

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

Gemini API API Key 
AQ.Ab8RN6LOhmRLmHvV1vUM1COhoHbK3M5npmevFCh1ffIlheDeng

Gemini API Friendly Eata Key

Project Name
projects/422968366405

Project Number
422968366405

To grant additional users access to this secret run firebase apphosting:secrets:grantaccess GEMINI_API_KEY --email [email list].
To grant additional backends access to this secret run firebase apphosting:secrets:grantaccess GEMINI_API_KEY --backend [backend ID]
